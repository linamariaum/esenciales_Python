# Para congelar la version de los paquetes que tenemos instalados dentro de nuestro proyecto

# > pip freeze > requirements.txt
# Esto crea un archivo requirements.txt con el listado de las dependencias usadas en el proyecto
